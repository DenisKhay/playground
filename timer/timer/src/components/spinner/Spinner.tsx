import React from "react";
import some from './spinner.module.scss';

export const Spinner = () => {
    return (
        <div className={some['foo-bar']}>
            hello am spinner
        </div>
    )
}