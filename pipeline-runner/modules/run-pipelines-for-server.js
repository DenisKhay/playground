
module.exports = (env) => {
    const {serverName, repos} = env;

    // we should run it for:

    // lmsu
    // msk
    // portals
    // coop


    const servers = {
        'qa3': '',
        'qa4': '',
        'uat': '',
        'ovflr': ''
    }

};